// const dotenv = require('dotenv');
// dotenv.config();


module.exports = {
	// port										: process.env.PORT || 8000,
	// mongoDBUrl   						: process.env.NODE_ENV === 'production' ? process.env.MONGO_ATLAS_URL : process.env.MONGO_BASE_URL,
	// mongoDBName   					: process.env.MONGO_DATABASE_NAME,
	// common_err_msg					: 'Something went wrong. Please try again later.',
	secret									: "1HGaXhTN19oNTme1fXnqXNZgPWB6RXjC",
	saltRounds							: 10,
};
